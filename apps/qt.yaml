name: qt
display_name: Qt (per-vendor app configs)
category: system
homepage: https://www.qt.io
locations:
  native:
    config:
    - ~/.config/QtProject
    - ~/.config/QtProject.conf
    - ~/.config/Trolltech.conf
    - ~/.config/Unknown Organization
  legacy:
    config: ~/.qt
tags:
- toolkit
- qt

name: bash
display_name: Bash
category: development
homepage: https://www.gnu.org/software/bash
locations:
  native:
    config:
      - ~/.bashrc
      - ~/.bash_profile
      - ~/.bash_logout
    state: ~/.bash_history
tags: [shell, cli]

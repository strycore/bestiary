name: konsole
display_name: Konsole
category: development
homepage: https://konsole.kde.org
locations:
  native:
    config:
    - ~/.config/konsolerc
    - ~/.config/konsolesshconfig
    data: ~/.local/share/konsole
    state: ~/.local/state/konsolestaterc
tags:
- terminal
- kde
